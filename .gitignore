
# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# bd audit residue: passive re-export, Dolt is source of truth
.beads/interactions.jsonl

# lint gate build/cache artifacts (bw-ajp)
/bin/
/.golangci-cache/

# session-local hook scratch (wrap markers, capture log)
/.claude/tmp/
